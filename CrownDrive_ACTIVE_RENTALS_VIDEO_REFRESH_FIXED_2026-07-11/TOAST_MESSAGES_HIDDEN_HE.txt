תיקון הודעות תחתונות:
- הודעות ה-Toast בתחתית המסך הוסתרו מהמשתמשים.
- הפונקציה showToast כבר לא מציגה בועה למטה.
- ההודעות עדיין נכתבות ל-console של הדפדפן לצורך בדיקות/דיבוג.
- לא נגעתי ב-Firebase / Auth / Realtime / Rules / PWA / Google verification.
