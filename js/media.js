import {auth} from './firebase.js';
async function token(){return auth.currentUser?.getIdToken()}
export async function uploadPrivate(file,kind,entityId=''){if(!file)throw Error('לא נבחר קובץ');const t=await token();if(!t)throw Error('נדרשת התחברות');const r=await fetch('/api/media-sign-upload',{method:'POST',headers:{'content-type':'application/json','authorization':`Bearer ${t}`},body:JSON.stringify({name:file.name,type:file.type,size:file.size,kind,entityId})});if(!r.ok)throw Error((await r.json()).error||'העלאה נכשלה');const {uploadUrl,path}=await r.json();const put=await fetch(uploadUrl,{method:'PUT',headers:{'content-type':file.type},body:file});if(!put.ok)throw Error('העלאת הקובץ נכשלה');return path}
export async function signedRead(path){const t=await token();const r=await fetch('/api/media-sign-read',{method:'POST',headers:{'content-type':'application/json','authorization':`Bearer ${t}`},body:JSON.stringify({path})});if(!r.ok)throw Error('אין הרשאה לקובץ');return (await r.json()).url}
