import {getAdmin,verify,json,canReadUserDocs} from './_firebase-admin.mjs';
export async function handler(event){try{if(event.httpMethod!=='POST')return json(405,{error:'Method not allowed'});const viewer=await verify(event),{uid}=JSON.parse(event.body||'{}');if(!uid||!await canReadUserDocs(viewer.uid,uid))return json(403,{error:'אין הרשאה לפרטי השוכר'});const db=getAdmin().database(),[p,d]=await Promise.all([db.ref(`users/${uid}`).once('value'),db.ref(`privateUserDocuments/${uid}`).once('value')]);const docs=d.val()||{},bucket=getAdmin().storage().bucket(),documents={};for(const [k,path] of Object.entries(docs)){const [url]=await bucket.file(path).getSignedUrl({version:'v4',action:'read',expires:Date.now()+5*60*1000});documents[k]=url}return json(200,{profile:p.val()||{},documents})}catch(e){console.error(e);return json(e.status||500,{error:e.message})}}
