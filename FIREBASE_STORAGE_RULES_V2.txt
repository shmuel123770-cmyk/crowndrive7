rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    // All browser access is denied. Files are accessed through short-lived,
    // authorization-checked signed URLs generated by Netlify Functions.
    match /{allPaths=**} {
      allow read, write: if false;
    }
  }
}
