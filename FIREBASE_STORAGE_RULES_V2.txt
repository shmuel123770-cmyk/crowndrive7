rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    // Browser SDK access is intentionally disabled.
    // Upload and read access use short-lived signed URLs issued by
    // Netlify Functions after Firebase ID-token and role checks.
    match /{allPaths=**} {
      allow read, write: if false;
    }
  }
}
