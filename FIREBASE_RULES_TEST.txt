Firebase Realtime Database temporary test rules for CrownDrive

Use only for testing. In Firebase Console > Realtime Database > Rules, paste:

{
  "rules": {
    ".read": true,
    ".write": true
  }
}

After confirming realtime works, replace these with locked rules.
